#include <stdio.h>

/**
 * main - inizialization of to print numbers
 * Return: Always 0
 */

int main(void)
{
	int num;

	while (num < 10)
	{
		printf("%d", num);
		num++;
	}
	printf('\n');
	return (0);
}
